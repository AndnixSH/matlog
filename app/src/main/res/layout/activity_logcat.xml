<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/main_background"
    xmlns:tools="http://schemas.android.com/tools"
    android:fitsSystemWindows="true">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?colorPrimary"
        android:elevation="4dp"
        android:fitsSystemWindows="false"
        app:layout_scrollFlags="scroll|enterAlways">

        <androidx.appcompat.widget.SearchView
            android:layout_width="match_parent"
            android:id="@+id/search_bar"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="8dp"
            android:layout_marginTop="8dp"
            app:iconifiedByDefault="false"
            app:queryBackground="@null"
            app:searchIcon="@drawable/ic_search"
            app:searchHintIcon="@drawable/ic_search"
            android:background="@drawable/search_background"/>

        <include
            android:id="@+id/find_bar"
            layout="@layout/find_bar"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="8dp"
            android:visibility="gone" />
    </com.google.android.material.appbar.AppBarLayout>

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/main_progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="8dp"
            android:indeterminate="true"
            android:visibility="gone"
            app:indicatorColor="?colorAccent"
            app:indicatorSize="96dp"
            app:trackThickness="8dp"
            tools:visibility="visible" />

        <androidx.recyclerview.widget.RecyclerView
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            android:id="@+id/list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:dividerHeight="0dp"
            android:paddingBottom="2dp"
            android:paddingTop="1dp"
            android:scrollbarSize="0dp"
            android:scrollbars="vertical"/>

    <!--
        The bar runs down to the bottom edge of the screen and pads its own
        content clear of the navigation bar or taskbar, so on edge-to-edge
        devices its colour fills the strip behind the gesture pill. Laid out
        above the insets instead, it left a band of the log area's colour
        underneath and looked like a card floating over the log. It takes
        three settings together:
        - fitsSystemWindows: the root CoordinatorLayout lays a child that fits
          system windows out edge to edge rather than inside the insets;
        - padding*SystemWindowInsets: the bar then keeps its menu row clear of
          the navigation bar itself;
        - wrap_content: that padding adds to the bar's height. Inside a fixed
          actionBarSize height it was taken out of the menu row and clipped
          the icons, which is why it had been switched off (commit 0079845).
        Below Android 15 the window is not edge to edge, the system paints the
        navigation bar itself and the inset is simply 0.

        No elevation: the bar is part of the screen edge, not something
        floating over the log. It slides away when the log is scrolled down,
        and LogcatActivity has the record button ride along in its cradle.
    -->
    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/bottom_appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:fitsSystemWindows="true"
        app:fabCradleRoundedCornerRadius="4dp"
        app:popupTheme="?popupMenuStyle"
        app:backgroundTint="?colorPrimary"
        app:hideOnScroll="true"
        app:elevation="0dp"
        app:paddingBottomSystemWindowInsets="true"
        app:paddingLeftSystemWindowInsets="true"
        app:paddingRightSystemWindowInsets="true"/>

    <!--
        No android:layout_gravity here on purpose. BottomAppBar only seats an
        anchored FAB into its cradle when the FAB's gravity is NO_GRAVITY; give
        it one and the cradle is still cut but the button is laid out by plain
        anchor+gravity instead, landing half a button-height below the notch.
    -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_stop_white_24dp"
        android:tint="@android:color/white"
        app:backgroundTint="#F44336"
        app:layout_anchor="@id/bottom_appbar"/>
</androidx.coordinatorlayout.widget.CoordinatorLayout>