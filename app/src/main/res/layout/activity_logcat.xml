<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/main_background"
    xmlns:tools="http://schemas.android.com/tools"
    android:fitsSystemWindows="true">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?colorPrimary"
        android:elevation="4dp"
        android:fitsSystemWindows="false"
        app:layout_scrollFlags="scroll|enterAlways">

        <androidx.appcompat.widget.SearchView
            android:layout_width="match_parent"
            android:id="@+id/search_bar"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="8dp"
            android:layout_marginTop="8dp"
            app:iconifiedByDefault="false"
            app:queryBackground="@null"
            app:searchIcon="@drawable/ic_search"
            app:searchHintIcon="@drawable/ic_search"
            android:background="@drawable/search_background"/>

        <include
            android:id="@+id/find_bar"
            layout="@layout/find_bar"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="8dp"
            android:visibility="gone" />
    </com.google.android.material.appbar.AppBarLayout>

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/main_progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="8dp"
            android:indeterminate="true"
            android:visibility="gone"
            app:indicatorColor="?colorAccent"
            app:indicatorSize="96dp"
            app:trackThickness="8dp"
            tools:visibility="visible" />

        <androidx.recyclerview.widget.RecyclerView
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            android:id="@+id/list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:dividerHeight="0dp"
            android:paddingBottom="2dp"
            android:paddingTop="1dp"
            android:scrollbarSize="0dp"
            android:scrollbars="vertical"/>

    <!--
        paddingBottomSystemWindowInsets is turned off on purpose: the root
        CoordinatorLayout already consumes the system-bar insets (it is
        fitsSystemWindows), so the app bar never actually receives them. Left
        on, BottomAppBar reserves navigation-bar padding inside its own fixed
        actionBarSize height anyway, which squeezes the menu row and clips the
        bottom off its icons.

        hideOnScroll is off as well. Sliding the bar away on scroll moved the
        anchored record button only by the bar's height, so its upper half
        stayed poking out of the bottom edge - most visibly on a tablet, where
        it looked cut off by the taskbar. The controls of a log viewer are
        better always reachable anyway.
    -->
    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/bottom_appbar"
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"
        android:layout_gravity="bottom"
        app:fabCradleRoundedCornerRadius="4dp"
        app:popupTheme="?popupMenuStyle"
        app:backgroundTint="?colorPrimary"
        app:hideOnScroll="false"
        app:elevation="4dp"
        app:paddingBottomSystemWindowInsets="false"/>

    <!--
        No android:layout_gravity here on purpose. BottomAppBar only seats an
        anchored FAB into its cradle when the FAB's gravity is NO_GRAVITY; give
        it one and the cradle is still cut but the button is laid out by plain
        anchor+gravity instead, landing half a button-height below the notch.
    -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_stop_white_24dp"
        android:tint="@android:color/white"
        app:backgroundTint="#F44336"
        app:layout_anchor="@id/bottom_appbar"/>
</androidx.coordinatorlayout.widget.CoordinatorLayout>