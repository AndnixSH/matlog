<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_LOGS"
        tools:ignore="ProtectedPermissions" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!--
        Continuous logcat capture is not one of the standard foreground service
        categories, so it uses specialUse. Deliberately NOT dataSync: that type
        is capped at 6 hours per 24h on API 35+, which would silently kill a
        long recording. specialUse has no time cap.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Shizuku: read logcat with shell privileges when root is unavailable. -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="${appName}"
        android:name=".App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.MatLog"
        tools:ignore="GoogleAppIndexingWarning">

        <!--TODO: REMOVE THIS CONFIGCHANGES ATROCITY-->
        <activity
            android:name="com.pluscubed.logcat.ui.LogcatActivity"
            android:configChanges="screenSize|orientation"
            android:exported="true"
            android:windowSoftInputMode="adjustPan"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <intent-filter>
                <action android:name="com.pluscubed.logcat.intents.LAUNCH"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts"/>
        </activity>

        <activity
            android:name="com.pluscubed.logcat.ui.AboutDialogActivity"
            android:exported="false"
            android:theme="@style/Theme.MatLog.AlertDialog"/>


        <activity
            android:name="com.pluscubed.logcat.ui.RecordLogDialogActivity"
            android:exported="false"
            android:theme="@style/Theme.MatLog.AlertDialog"/>


        <activity
            android:name="com.pluscubed.logcat.ui.SettingsActivity"
            android:exported="false"/>

        <service
            android:name="com.pluscubed.logcat.LogcatRecordingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Continuous logcat capture written to a user-selected file"/>
        </service>

        <service
            android:name="com.pluscubed.logcat.CrazyLoggerService"
            android:exported="false"/>

        <receiver
            android:name="com.pluscubed.logcat.RecordingWidgetProvider"
            android:exported="true">

            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
                <data android:scheme="catlog_widget"/>
            </intent-filter>

            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>

            <intent-filter>
                <action android:name="${applicationId}.action.RECORD_OR_STOP"/>
                <data android:scheme="catlog_widget"/>
            </intent-filter>


            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/recording_widget_info"/>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

        <meta-data
            android:name="moe.shizuku.client.V3_SUPPORT"
            android:value="true" />

        <!--
            Shizuku's client provider. It is what delivers the Shizuku binder to
            the app; the INTERACT_ACROSS_USERS_FULL permission keeps other apps
            from talking to it.
        -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
    </application>
</manifest>
